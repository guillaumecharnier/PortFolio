{% extends 'base.html.twig' %}

{% block title %}Présentation{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-body">
                <h2>Présentation</h2>
                <p>Je suis Guillaume Charnier, développeur passionné par les technologies web et mobiles.</p>
                <p>J'ai une expérience solide avec PHP, Symfony, JavaScript et bien d'autres technologies.</p>
                <p>N'hésitez pas à me contacter pour discuter de vos projets et de mes compétences !</p>
            </div>
        </div>
    </div>

    <div class="card-body">
        <div id="btn-container" class="text-center">
            <a id="btn-explorer" class="btn btn-primary btn-lg" href="{{ path('app_show_PortFolio') }}" role="button">Retour au menu</a>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            background-color: #222222;
            color: #ffffff;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .container {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .card {
            max-width: 800px;
            width: 100%;
            padding: 20px;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            color: #333333;
            margin-bottom: 20px; /* Espacement entre la carte et le footer */
            border: 1px solid #cccccc; /* Bordure légère */
        }

        .card-body {
            padding: 20px;
        }

        h2 {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        p {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        /* Style pour le footer */
        .footer {
            text-align: right; /* Alignement à droite */
            color: #ffffff; /* Couleur du texte */
            margin-top: 20px; /* Espacement entre le contenu principal et le footer */
        }

        .footer-text {
            font-size: 0.9rem; /* Taille du texte du footer */
        }

        .footer-link {
            color: #3498db; /* Couleur du lien */
            text-decoration: none; /* Supprimer la décoration du lien */
            margin-left: 20px; /* Espacement entre les éléments du footer */
            transition: color 0.3s ease-out; /* Transition sur le survol */
        }

        .footer-link:hover {
            color: #2980b9; /* Couleur du texte au survol */
        }
    </style>
{% endblock %}

{% block footer %}
    <div class="footer">
        <p class="footer-text">&copy; 2024 Guillaume Charnier - Tous droits réservés <a href="{{ path('app_mentions_legales') }}" class="footer-link">Mentions légales</a></p>
    </div>
{% endblock %}
